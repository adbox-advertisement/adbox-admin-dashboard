@AGENTS.md

# Claude Code

The imported instructions are shared with Codex. Update `AGENTS.md` for project-wide rules
instead of duplicating them here. Read `docs/AI_HANDOFF.md` before continuing previous work.

Launch from this repository with `npm run ai:claude`. Keep personal settings in
`CLAUDE.local.md` or `.claude/settings.local.json`; both are ignored by Git.
